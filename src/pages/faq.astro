---
import '../styles/global.css';

const pageTitle = "常见问题";
const manualTitle = "AUTO_OJ // 操作手册";

// FAQ 数据
const faqs = [
  {
    question: "如何提交代码？",
    answer: "在题目页面点击'提交代码'按钮，将你的代码粘贴到编辑器中，选择对应的编程语言，然后点击提交即可。你也可以直接上传代码文件。"
  },
  {
    question: "提交后多久能看到结果？",
    answer: "通常情况下，代码会在 1-5 秒内完成评测。如果评测队列较长，可能需要等待更长时间。请耐心等待，不要重复提交。"
  },
  {
    question: "评测结果中的状态是什么意思？",
    answer: `评测系统有以下几种状态：
    <ul>
      <li><strong>Accepted (AC)</strong>：答案正确，所有测试用例通过</li>
      <li><strong>Wrong Answer (WA)</strong>：答案错误</li>
      <li><strong>Time Limit Exceeded (TLE)</strong>：运行超时</li>
      <li><strong>Memory Limit Exceeded (MLE)</strong>：内存超限</li>
      <li><strong>Compilation Error (CE)</strong>：编译错误</li>
      <li><strong>Runtime Error (RE)</strong>：运行时错误</li>
    </ul>`
  },
  {
    question: "为什么我的代码是 Wrong Answer？",
    answer: `Wrong Answer 表示你的程序输出了错误的结果。可能的原因包括：
    <ul>
      <li>逻辑错误：算法思路不正确</li>
      <li>边界条件：没有处理特殊情况（如空输入、极值等）</li>
      <li>输出格式：输出格式与题目要求不一致（多了空格、换行等）</li>
      <li>数据类型：使用了错误的数据类型导致精度问题</li>
    </ul>
    建议仔细检查代码，并用测试用例验证。`
  },
  {
    question: "如何查看测试用例？",
    answer: "评测结束后，你可以点击查看详情来看到每个测试用例的输入、预期输出和你的实际输出。这有助于快速定位问题所在。"
  },
  {
    question: "可以重复提交吗？",
    answer: "可以。你可以多次提交同一题目，系统会记录所有提交，最终成绩取所有提交中的最高分。但建议在本地充分测试后再提交，避免占用评测资源。"
  },
  {
    question: "支持哪些编程语言？",
    answer: `系统目前支持以下编程语言：
    <ul>
      <li>C (gcc)</li>
      <li>C++ (g++)</li>
      <li>Java (OpenJDK)</li>
      <li>Python 3</li>
      <li>其他语言请查看系统公告</li>
    </ul>`
  },
  {
    question: "如何提高代码效率？",
    answer: `提高代码效率的一些建议：
    <ul>
      <li>选择合适的数据结构（如用数组代替链表）</li>
      <li>优化算法（如用更快的排序算法）</li>
      <li>减少不必要的循环嵌套</li>
      <li>使用缓存避免重复计算</li>
      <li>使用快速 I/O 方法</li>
    </ul>`
  },
  {
    question: "遇到系统错误怎么办？",
    answer: "如果遇到系统错误（如评测系统崩溃、页面无法加载等），请截图保存错误信息，然后联系管理员或通过系统反馈渠道报告问题。"
  },
  {
    question: "如何联系技术支持？",
    answer: "你可以通过以下方式获取帮助：\n1. 查看系统日志页面获取更多信息\n2. 在讨论区发帖提问\n3. 发送邮件至 support@auto-oj.example.com\n4. 加入官方 QQ 群或微信群"
  }
];
---

<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{pageTitle} - {manualTitle}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
    <style>
      .faq-item {
        background: rgba(10, 10, 10, 0.9);
        border: 1px solid var(--color-neon-cyan);
        border-radius: 4px;
        margin-bottom: 15px;
        overflow: hidden;
      }
      .faq-question {
        background: rgba(0, 255, 255, 0.1);
        padding: 15px 20px;
        cursor: pointer;
        display: flex;
        justify-content: space-between;
        align-items: center;
        transition: background 0.3s;
      }
      .faq-question:hover {
        background: rgba(0, 255, 255, 0.2);
      }
      .faq-question strong {
        color: var(--color-neon-acid);
        font-size: 16px;
      }
      .faq-toggle {
        color: var(--color-neon-cyan);
        font-size: 20px;
        transition: transform 0.3s;
      }
      .faq-toggle.open {
        transform: rotate(45deg);
      }
      .faq-answer {
        padding: 0 20px;
        max-height: 0;
        overflow: hidden;
        transition: max-height 0.3s ease-out, padding 0.3s ease;
      }
      .faq-answer.open {
        padding: 20px;
        max-height: 1000px;
      }
      .faq-answer ul {
        margin-top: 10px;
        margin-left: 20px;
      }
      .faq-answer li {
        margin-bottom: 8px;
      }
      @media (max-width: 768px) {
        .faq-question {
          padding: 12px 15px;
        }
        .faq-question strong {
          font-size: 14px;
        }
      }
    </style>
  </head>
  <body>
    <div class="cyber-shell">
      <!-- Header -->
      <header class="cyber-header">
        <div class="brand">{manualTitle}</div>
        <a href="/" class="menu-btn" style="width: auto; padding: 8px 20px; text-decoration: none;">&larr; 返回首页</a>
      </header>

      <!-- Sidebar -->
      <aside class="cyber-sidebar">
        <div class="section-title">快速导航</div>
        <a href="/intro" class="menu-btn">系统介绍</a>
        <a href="/schedule" class="menu-btn">日程安排</a>
        <a href="/grading" class="menu-btn">评分标准</a>
        <a href="/faq" class="menu-btn active">常见问题</a>
        <a href="/" class="menu-btn" style="border-color: var(--color-neon-pink); color: var(--color-neon-pink);">[ 返回手册 ]</a>
      </aside>

      <!-- Main Content -->
      <main class="cyber-main" style="overflow-y: auto; padding: 40px;">
        <div class="content-container">
          <section class="content-section">
            <h2>常见问题解答</h2>
            <p>这里收集了用户最常遇到的问题和解答。如果找不到答案，请通过系统反馈渠道联系我们。</p>
          </section>

          <section class="content-section">
            <h2>问题列表</h2>

            {faqs.map((faq, index) => (
              <div class="faq-item">
                <div class="faq-question" onclick={`toggleFaq(${index})`}>
                  <strong>{faq.question}</strong>
                  <span class="faq-toggle" id={`toggle-${index}`}>+</span>
                </div>
                <div class="faq-answer" id={`answer-${index}`}>
                  <p set:html={faq.answer}></p>
                </div>
              </div>
            ))}
          </section>

          <section class="content-section">
            <h2>更多帮助</h2>
            <p>如果你没有找到答案，可以通过以下方式获取帮助：</p>
            <ul>
              <li>查看<a href="/logs" style="color: var(--color-neon-cyan);">系统日志</a>获取更多信息</li>
              <li>在讨论区发帖提问</li>
              <li>联系技术支持团队</li>
            </ul>
          </section>
        </div>
      </main>
    </div>

    <script is:inline>
      function toggleFaq(index) {
        const answer = document.getElementById(`answer-${index}`);
        const toggle = document.getElementById(`toggle-${index}`);

        answer.classList.toggle('open');
        toggle.classList.toggle('open');
      }
    </script>
  </body>
</html>
